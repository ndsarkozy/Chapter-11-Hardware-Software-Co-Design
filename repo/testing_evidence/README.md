# Testing Evidence

This folder holds evidence that the module works as intended. Expected contents once the module is complete:

- `step1_canonical_scope.png` — scope capture showing ~1 Hz blink with the actual slight frequency offset
- `step2_software_failure.png` — scope capture showing WS2812 timing breakdown under background load
- `step4_rmt_success.png` — scope capture of clean RMT output under the same load
- `grading_tests_passing.txt` — stdout from `python3 tests/test_grading.py`
- `classroom_system_walkthrough/` — screenshots of the lesson running inside the real classroom system

None of these files are in the repo yet because the testing hasn't happened yet. As each step is verified end-to-end, evidence lands here.
